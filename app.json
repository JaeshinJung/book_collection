{
    "environments": {
        "test": {
            "addons": [
                "heroku-postgresql:in-db"
            ],
            "env": {
                "GOOGLE_OAUTH_CLIENT_ID": {
                    "required": true
                },
                "GOOGLE_OAUTH_CLIENT_SECRET": {
                    "required": true
                }
            }
        }
    }
}